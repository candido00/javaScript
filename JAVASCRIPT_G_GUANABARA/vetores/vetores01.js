let numeros = [9, 4, 2, 4, 8, 1];
numeros.sort();
/*console.log(numeros);
console.log();
for(let posi = 0; posi < numeros.length; posi++){
    console.log(numeros[posi]);
}*/

for(let posi in numeros){
    console.log(numeros[posi]);
}
